// 2. Создай функцию, которая принимает два параметра с одинаковыми именами.
// Включи строгий режим и исправь ошибку, чтобы функция работала корректно;

// "use strict";
// При строгом режиме не может быть 2 одинаковых параметра будет ошибка


// function getNumber(a, a) {
//     sum = a + a;
//     return sum
// }
//
// console.log(getNumber(1, 1))


function getNumber(a, a) {
    sum = a + a;
    return sum
}

console.log(getNumber(1, 1))
