const nameSity = 'Moscow'; // Названия города
const temperature = 20; // Температура на сегодня
const sityTemper = `${nameSity} +${temperature}`; // Конкатенация двух значений

console.log(sityTemper) // Вывод в консоль результата


const a = 5; // Первая переменная
const b = 5; // Вторая переменная

const multiplication = a * b; // Умножения двух переменных

console.log(multiplication) // Вывод в консоль результата
